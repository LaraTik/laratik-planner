# External-services UAT

> **Purpose.** Owner-completed evidence for the external-service checks
> called out in `STUDIOFLOW_MASTER_PROMPT.md` §23/§24 and
> `PRODUCTION_READINESS_TRACKER.md` (rows OBS-001, OPS-001, AI-001).
> These are checks that **cannot be automated** because they require a
> real Google OAuth client, a real Mailcow account, a real Anthropic
> API key, a real Sentry project, an offsite backup repository, and
> a credentials vault. The build, typecheck, unit, and axe-core
> automated gates are recorded separately in
> [`TEST_EVIDENCE.md`](./TEST_EVIDENCE.md) and
> [`SECURITY_AUDIT.md`](./SECURITY_AUDIT.md); this file is the
> controlled end-to-end sign-off for each external dependency.
>
> **Rules.**
>
> 1. Record only operator, date, environment and result. Never record
>    secret values, real invitation URLs, raw session tokens, or
>    production user data (per
>    `docs/production-readiness/README.md` evidence rule 2).
> 2. The operator for a check is named in the **Owner** column at
>    the time the check is run; if ownership changes, the new owner
>    updates the column.
> 3. The next rotation date in the credential-rotation section is
>    the date the credential is **next** rotated, not the date it
>    was last rotated (the rotation history is in
>    `MIGRATION_DEPLOYMENT.md` / `OPS-001` evidence in the tracker).
> 4. `Pass` requires the operator to have observed the documented
>    behaviour in the named environment on the named date; `Fail`
>    requires a tracking issue link; `Blocked` requires the owner
>    and the external action.

## Pass criteria

A row may be marked `Pass` only when the operator observed the
documented behaviour against a real external account on the named
date, the automated contract for the same surface is still green
(see `TEST_EVIDENCE.md`), and no tracking issue is open. `Fail`
must link to a tracking issue with a reproduction. `Blocked` must
name the owner and the external action required (typically a
missing credential or an external account that the owner has not
provisioned yet). The independent reviewer flips `Pass` rows to
`Verified`; this file never self-assigns `Verified`.

## Auto-check columns (added in 2026-08-22 pre-fill)

The first three columns under each section are auto-verified by
reading the code and the test surface. The remaining columns
(`Owner`, `Environment`, `Date`, `Result`, `Evidence link`) still
require the named owner to run the check against a real external
account on the named date.

- `Auto-check: code path exists`:
  - `PASS` — the implementation is present and uncommented in the
    code (e.g. the Google provider is mounted in
    `src/lib/auth/config.ts`; the Sentry wrapper is wired in
    `src/lib/observability/sentry.ts` and `next.config.ts`).
  - `MISSING` — the implementation is absent, stubbed, or
    commented out (e.g. the restic offsite backup block in
    `scripts/vps/backup.sh` is currently commented out behind a
    TODO).
  - `STUBBED` — the implementation is partially present but only
    no-ops or short-circuits (e.g. the AI module returns `null`
    when disabled, but there is no canned-response fake).
- `Auto-check: unit test covers`:
  - `PASS` — a unit or integration test pins the named contract
    (transport wiring, scrubber, workflow rule, etc.).
  - `PARTIAL` — a test covers a subset of the named contract
    (e.g. the `isAiEnabled` test but not the full route handler).
  - `MISSING` — no test exercises the contract; the reviewer
    must run the row manually.
- `Auto-check: local-fake mode works` — for AI / Sentry / OAuth
  the codebase has a dev-mode fake that lets the app run
  end-to-end without a real external account:
  - `PASS` — the dev-mode fake is wired and exercised by tests
    (e.g. dev sign-in bypasses Google; the Sentry wrapper is
    a no-op without a DSN; the AI module returns `null` and
    the API route returns 503 when `AI_FEATURE_ENABLED=false`).
  - `MISSING` — there is no dev-mode fake for this external
    service; the row requires a real credential to exercise.

## Google OAuth

| #   | Service      | Auto-check: code path exists                                                                                                                                                                                                                                                                 | Auto-check: unit test covers                                                                                                                                                          | Auto-check: local-fake mode works                                                                                                                                                                                              | Check                                                                                                                                                                                  | Owner                  | Environment                                | Date | Result | Evidence link |
| --- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------ | ---- | ------ | ------------- |
| 1   | Google OAuth | PASS (`src/lib/auth/config.ts` lines 72-82: `Google({…})` provider mounted when `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` are set; `src/app/api/auth/[...nextauth]/route.ts` re-exports `handlers`)                                                                                        | PASS (`tests/unit/auth-config.test.ts` pins the provider list + the Google-only branch)                                                                                               | PASS (dev sign-in at `src/app/api/dev/sign-in/route.ts` + `src/lib/auth/dev-sign-in.ts` bypasses Google; `tests/e2e/_helpers.ts` `bootstrapRoleSession` exercises it; `tests/unit/dev-sign-in.test.ts` pins the dev-only gate) | Redirect URL — `/api/auth/signin/google` produces a valid `accounts.google.com` consent URL with the production `client_id`, `redirect_uri` and PKCE `state`                           | Implementation / Owner | production (`https://planner.laratik.com`) |      |        |               |
| 2   | Google OAuth | PASS (same provider; `authorize` callback in `src/lib/auth/config.ts` returns the verified user; the `DrizzleAdapter` writes the linked `account` row)                                                                                                                                       | PARTIAL (`tests/unit/auth-config.test.ts` covers the provider-config + `allowDangerousEmailAccountLinking: false`; no e2e exercises the real Google redirect)                         | PASS (dev sign-in is the local fake for the OAuth callback; the operator can drive the same downstream page transitions via `bootstrapRoleSession`)                                                                            | Callback exchange — the Google redirect back to `/api/auth/callback/google` returns `200` with a session cookie; the `code` is exchanged exactly once; replay fails closed             | Implementation / Owner | production                                 |      |        |               |
| 3   | Google OAuth | PASS (`src/lib/auth/config.ts` `session: { strategy: "jwt", maxAge: 30 * 24 * 60 * 60 }` + JWT callbacks at lines 126-146 that persist `id` and `role` into the cookie; `tests/unit/security-headers.test.ts` pins the cookie flags via the response `Set-Cookie` header on the signin page) | PARTIAL (JWT shape pinned in `tests/unit/auth-config.test.ts`; cookie flags pinned in `tests/unit/security-headers.test.ts`; no e2e asserts the live `GET /api/auth/session` payload) | PASS (dev sign-in + `bootstrapRoleSession` produce a JWT cookie with the same shape as the Google sign-in path)                                                                                                                | Session establishment — the session cookie sets `httpOnly`, `secure` and `sameSite=lax`; subsequent `GET /api/auth/session` returns the expected user identity (no role/email leakage) | Implementation / Owner | production                                 |      |        |               |

## Mailcow SMTP

| #   | Service      | Auto-check: code path exists                                                                                                                                                                                                                                                     | Auto-check: unit test covers                                                                                                                                                                                                                                                                                | Auto-check: local-fake mode works                                   | Check                                                                                                                                                                                                                                       | Owner                  | Environment | Date | Result | Evidence link |
| --- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ----------- | ---- | ------ | ------------- |
| 1   | Mailcow SMTP | PASS (`src/lib/auth/invitations.ts` lines 122-130: `sendEmail({to, subject, text, …})` with the accept URL; lines 132-137 invalidate the prior pending invitation by setting `status: "pending"` + writing `securityAuditEvents`)                                                | PASS (`tests/unit/email.test.ts` pins the SMTP transport + template fields; `tests/integration/invitation-concurrency.test.ts` pins the `for("update")` lock and the accept-idempotency; `tests/unit/invitation-command.test.ts` pins the input schema)                                                     | N/A (no dev-fake for SMTP; rows 1-3 require a real Mailcow account) | Invitation delivery — `POST /api/invitations` triggers a templated email to the new member's address; the email contains the invitation URL, the workspace name and a single-use token; the token is invalidated after first acceptance     | Implementation / Owner | production  |      |        |               |
| 2   | Mailcow SMTP | PASS (`src/lib/auth/config.ts` lines 83-96: `Nodemailer({ server: { host, port, auth }, from })` provider mounted when `SMTP_HOST` + `SMTP_USER` + `SMTP_PASSWORD` + `SMTP_FROM` are set; NextAuth's built-in `sendVerificationRequest` writes the magic-link to that transport) | PARTIAL (`tests/unit/email.test.ts` pins the transport; the NextAuth-internal magic-link transport is not directly unit-tested because NextAuth owns the `sendVerificationRequest` impl)                                                                                                                    | N/A (no dev-fake)                                                   | Magic-link delivery — the email magic-link request flow sends a sign-in link to the address on file; the link is single-use, scoped to the next sign-in attempt, and expires within 15 minutes                                              | Implementation / Owner | production  |      |        |               |
| 3   | Mailcow SMTP | PASS (`src/app/signin/forgot-password/page.tsx` lines 21-60: `requestResetAction` calls `issuePasswordResetToken` and `sendEmail` with the reset URL; line 27/42/59 `redirect("/signin/forgot-password?sent=1")` is identical whether or not the user exists)                    | PASS (`tests/unit/email.test.ts` pins the transport; `tests/unit/auth-password-hash.test.ts` `describe("issuePasswordResetToken")` pins the 1-hour expiry + the row insert; the same describe block pins `consumePasswordResetToken` returning `null` for empty / short / expired / mismatched-hash tokens) | N/A (no dev-fake)                                                   | Password-reset delivery — `/signin/forgot-password` sends a reset link to the address on file; the link is single-use, expires within 1 hour, and cannot enumerate accounts (response shape is identical whether the address exists or not) | Implementation / Owner | production  |      |        |               |

## MiniMax (Anthropic) AI

| #   | Service    | Auto-check: code path exists                                                                                                                                                                                                                                                                                                                  | Auto-check: unit test covers                                                                                                                                                                                                                                                                                       | Auto-check: local-fake mode works                                                                                                                                                                                                                                                               | Check                                                                                                                                                                                                                                                        | Owner                  | Environment | Date | Result | Evidence link |
| --- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------- | ----------- | ---- | ------ | ------------- |
| 1   | MiniMax AI | PASS (`src/lib/ai/index.ts` lines 39-81: `chat()` POSTs to `${MINIMAX_BASE_URL}/v1/messages` with `x-api-key` + `anthropic-version: 2023-06-01` headers; `src/app/api/ai/generate/route.ts` lines 99-166 read the content item, enforce the workspace-role + capability allowlist, and write a row to `aiUsageEvents` with `succeeded: true`) | PASS (`tests/unit/ai.test.ts` `describe("chat")` pins the URL, headers, body, and JSON parsing; `tests/unit/rate-limit-policy.test.ts` pins the `ai_generation` rate-limit scope; `tests/unit/public-error.test.ts` pins the `publicProviderError("ai", …)` shape)                                                 | PASS (the `isAiEnabled()` short-circuit returns `null` for `chat()` / `draftCaption()` / `improveBrief()` / `checkCompleteness()`; the route returns 503; the `AI-Assistance` section in `src/app/(app)/app/w/[slug]/planning/[id]/ai-assistance-section.tsx` is hidden when `aiLive` is false) | Enabled path — with `AI_ENABLED=true` and a valid API key, the `POST /api/ai/generate` route returns a 2xx response with the model output; `aiUsageEvents` records one row with the model, prompt and completion token counts; rate-limit policy is enforced | Implementation / Owner | production  |      |        |               |
| 2   | MiniMax AI | PASS (same `isAiEnabled()` short-circuit + the `isAiEnabled` check at the top of the route handler)                                                                                                                                                                                                                                           | PASS (`tests/unit/ai.test.ts` `describe("isAiEnabled")` pins the 3 cases: flag-off, key-missing, both-on)                                                                                                                                                                                                          | PASS (same as row 1 — the disabled state is the local fake)                                                                                                                                                                                                                                     | Disabled path — with `AI_ENABLED=false` (or no API key), the `POST /api/ai/generate` route returns 4xx with a user-visible "AI is disabled" message; the workflow remains usable (no `generate` button on disabled surfaces)                                 | Implementation / Owner | production  |      |        |               |
| 3   | MiniMax AI | PASS (`src/lib/ai/index.ts` lines 60-63: `throw new Error("MiniMax API error: …")`; `src/app/api/ai/generate/route.ts` lines 167-175: `catch (e) { logError("ai.provider_failed", …); return NextResponse.json({ error: publicProviderError("ai", e).message }, { status: 502 }) }`)                                                          | PARTIAL (`tests/unit/ai.test.ts` pins the `chat()` throw on non-2xx; `tests/unit/public-error.test.ts` pins `publicProviderError("ai", cause)` strips the upstream body; the route-level `try/catch` + `logError` + 502 mapping is not directly unit-tested — the integration journey exercises the disabled path) | N/A (the error path requires a fake-upstream to exercise locally; no canned-error fake is wired)                                                                                                                                                                                                | Provider error path — when the upstream returns a 5xx or the response parse fails, the route returns a redacted 502; `public-error` ensures no provider detail leaks; `aiUsageEvents` records the failure row                                                | Implementation / Owner | production  |      |        |               |

## Sentry

| #   | Service | Auto-check: code path exists                                                                                                                                                                                                                                                                                                      | Auto-check: unit test covers                                                                                                                                                                                                              | Auto-check: local-fake mode works                                                                                                                                                                                                                                   | Check                                                                                                                                                                                                                                                                       | Owner | Environment | Date | Result | Evidence link |
| --- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------- | ---- | ------ | ------------- |
| 1   | Sentry  | PASS (`src/lib/observability/sentry.ts` lines 44-50: `Sentry.init({ dsn, …, release: process.env.SENTRY_RELEASE, environment })`; `next.config.ts` lines 47-54: `withSentryConfig` reads `SENTRY_ORG` / `SENTRY_PROJECT` / `SENTRY_AUTH_TOKEN` and tags the release from the build's commit SHA via the `Sentry` CLI integration) | PASS (`tests/unit/observability-sentry.test.ts` `describe("sentry wrapper (DSN + SDK-loaded path)")` pins `init` is called with `{ dsn, release, environment, tracesSampleRate }`; the release is read from `process.env.SENTRY_RELEASE`) | PASS (when `SENTRY_DSN` is unset, `loadSentry()` returns `noopSentry` at line 36; `tests/unit/observability-sentry.test.ts` `describe("sentry wrapper (no DSN path)")` pins this; `tests/unit/sentry.test.ts` pins `captureException` does not throw without a DSN) | Release tagging — every production deploy writes a Sentry release with the exact commit SHA; the release is visible in the Sentry UI alongside its source maps                                                                                                              | Owner | production  |      |        |               |
| 2   | Sentry  | PASS (`next.config.ts` line 52: `sourcemaps: { deleteSourcemapsAfterUpload: true }` inside the `withSentryConfig` options; `Sentry` CLI upload happens in the Docker build stage when `SENTRY_AUTH_TOKEN` is set)                                                                                                                 | PARTIAL (`tests/unit/observability-sentry.test.ts` pins the `init` options; the build-time `deleteSourcemapsAfterUpload` is a `withSentryConfig` argument that the unit tests do not exercise — the build pipeline is not unit-tested)    | PASS (no-op DSN path lets the dev app run with `@sentry/nextjs` never loaded)                                                                                                                                                                                       | Source maps — stack traces in the Sentry UI for a production error show the original TypeScript frame, not the minified bundle; source maps are deleted from the upload pipeline after upload (per `next.config.ts` `withSentryConfig`)                                     | Owner | production  |      |        |               |
| 3   | Sentry  | PASS (`src/lib/observability/logger.ts` lines 1-13: `sanitizeLogContext` recursively scrubs every key matching `/authorization\|cookie\|secret\|token\|password\|api.?key\|brief\|body\|content\|prompt/i`; `tests/unit/observability-logger.test.ts` `describe("sanitizeLogContext")` exercises nested objects + arrays + Error) | PASS (same `sanitizeLogContext` test)                                                                                                                                                                                                     | PASS (logger runs in every env; the scrubber is pure-function and does not need Sentry at all)                                                                                                                                                                      | Data scrubbing — a deliberately injected `Authorization: Bearer <test-token>` header and a deliberate `password=<test-secret>` request body do **not** appear in the captured event payload in the Sentry UI; `lib/observability/logger.ts` recursive scrubber catches both | Owner | production  |      |        |               |
| 4   | Sentry  | MISSING (alert delivery is configured in the Sentry UI, not in this repo; the alert rule is owned by the Sentry project and the on-call channel is owned by the operator; no code path exercises alert delivery)                                                                                                                  | MISSING (no test covers alert delivery)                                                                                                                                                                                                   | N/A (alert delivery is a Sentry UI + on-call channel config; no local fake)                                                                                                                                                                                         | Alert delivery — a deliberately triggered error sends the configured alert to the on-call channel within the Sentry alert-rule SLA; the alert includes the release SHA and the affected route                                                                               | Owner | production  |      |        |               |

## Encrypted offsite backup

| #   | Service                  | Auto-check: code path exists                                                                                                                                                                                                                  | Auto-check: unit test covers                                                                                                                           | Auto-check: local-fake mode works | Check                                                                                                                                                                                                                                  | Owner | Environment                    | Date | Result | Evidence link |
| --- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------ | ---- | ------ | ------------- |
| 1   | Encrypted offsite backup | STUBBED (`scripts/vps/backup.sh` lines 30-37 contain the restic `backup` + `forget` block, currently commented out pending `/root/.config/restic/env` and the offsite repo URL; local pg_dump + sha256 + 14-day prune is live at lines 14-29) | PARTIAL (`scripts/migration-drill.ts` lines 345-396 cover the pg_dump → drop → psql → smoke-query path for the _local_ backup, not the restic offsite) | N/A (no local fake for offsite)   | Production backup — the daily backup job writes a restic-encrypted snapshot to the offsite repo; the snapshot contains the full database and the private storage volume; the snapshot is verifiable (`restic check` reports no errors) | Owner | production                     |      |        |               |
| 2   | Encrypted offsite backup | MISSING (no code path performs a disposable restore from the offsite repo; `scripts/migration-drill.ts` exercises the local pg_dump ↔ psql round-trip but not restic ↔ Postgres)                                                              | PARTIAL (same `scripts/migration-drill.ts` coverage as row 1)                                                                                          | N/A                               | Timed disposable restore — a fresh throwaway Postgres instance is restored from the latest offsite snapshot; total wall-clock time (download + restore + connect + smoke query) is recorded and is under the agreed RTO                | Owner | production (disposable target) |      |        |               |

## Credential rotation

> All 5 rows below are external owner actions with no in-repo code
> path. The Owner column must be filled by the named human and the
> next-rotation date is recorded in
> [`MIGRATION_DEPLOYMENT.md`](./MIGRATION_DEPLOYMENT.md) § OPS-001.

| #   | Service      | Auto-check: code path exists                                                                                                                                                                                         | Auto-check: unit test covers                                | Auto-check: local-fake mode works | Check                                                                                                                                                     | Owner | Environment | Date | Result | Evidence link |
| --- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------- | ---- | ------ | ------------- |
| 1   | Database     | MISSING (rotation is an out-of-band operator action against the managed Postgres provider + the secret store; no in-repo automation; the migration-drill script verifies the schema, not the credential swap)        | MISSING (no test covers a DB password rotation)             | N/A                               | Credential rotation — production DB username/password rotated; old credential revoked in lockstep; next rotation date                                     | Owner | production  |      |        |               |
| 2   | Google OAuth | MISSING (same — rotation is an operator action against the Google Cloud console + the secret store; `src/lib/auth/config.ts` reads `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` from `serverEnv` on every cold start) | MISSING (no test covers an OAuth client secret rotation)    | N/A                               | Credential rotation — production OAuth client id/secret rotated; new credentials pushed via the secret store; old credentials revoked; next rotation date | Owner | production  |      |        |               |
| 3   | Mailcow SMTP | MISSING (operator action against the Mailcow admin UI; `src/lib/email/index.ts` reads `SMTP_PASSWORD` from `serverEnv` on every cold start)                                                                          | MISSING (no test covers an SMTP password rotation)          | N/A                               | Credential rotation — production SMTP password rotated; old password revoked at the provider; next rotation date                                          | Owner | production  |      |        |               |
| 4   | Sentry       | MISSING (operator action against the Sentry UI; `src/lib/observability/sentry.ts` reads `SENTRY_DSN` from `process.env` on every cold start; `next.config.ts` reads `SENTRY_AUTH_TOKEN` at build time)               | MISSING (no test covers a Sentry DSN / auth-token rotation) | N/A                               | Credential rotation — production Sentry DSN and auth token rotated; old token revoked; next rotation date                                                 | Owner | production  |      |        |               |
| 5   | MiniMax AI   | MISSING (operator action against the Anthropic console; `src/lib/ai/index.ts` reads `MINIMAX_API_KEY` from `serverEnv` on every cold start)                                                                          | MISSING (no test covers an Anthropic API key rotation)      | N/A                               | Credential rotation — production MiniMax API key rotated; old key revoked; next rotation date                                                             | Owner | production  |      |        |               |

### Auto-check coverage (2026-08-22 pre-fill on `96e7048`)

- **Code path exists** — 12 of 20 rows are `PASS` (Google OAuth 1-3,
  Mailcow 1-3, MiniMax AI 1-3, Sentry 1-3), 1 is `STUBBED`
  (encrypted offsite backup row 1: restic block is commented out in
  `scripts/vps/backup.sh` lines 30-37), 7 are `MISSING` (Sentry
  row 4 alert delivery, offsite row 2 disposable restore, and all
  5 credential-rotation rows because rotation is an external
  operator action with no in-repo automation).
- **Unit test covers** — 7 rows are `PASS` (Google OAuth 1, Mailcow
  1 + 3, AI 1 + 2, Sentry 1 + 3), 7 are `PARTIAL` (Google OAuth 2 +
  3, Mailcow 2, AI 3, Sentry 2, offsite 1 + 2), 6 are `MISSING`
  (Sentry 4 + all 5 credential-rotation rows). The `MISSING` cluster
  is the credential-rotation section (no test covers a credential
  rotation by design — it's an external action) and the Sentry
  alert-delivery row (no test exercises a real Sentry alert rule).
- **Local-fake mode** — 8 rows are `PASS` (Google OAuth 1, 2, 3 via
  the dev sign-in endpoint; AI 1, 2 via the `isAiEnabled()`
  short-circuit and the route's 503; Sentry 1, 2 via the no-DSN
  no-op wrapper; Sentry 3 via the pure `sanitizeLogContext`
  scrubber), 12 are `N/A` because the dev-fake is not the contract
  the reviewer is signing off on (they sign off on the real
  external service).
- **pending owner** — `Owner`, `Date`, `Environment`, `Result` and
  `Evidence link` are still empty in every row. The named human
  fills those against a real external account on the named date;
  an empty `Result` is the expected starting state.

## How this file links to the rest of the evidence

- Automated contracts:
  - `tests/e2e/a11y-routes.spec.ts` (axe-core per route, mirrored in
    [`TEST_EVIDENCE.md`](./TEST_EVIDENCE.md))
  - `tests/integration/invitation-concurrency.test.ts` (invitation
    invariant, mirrored in `SECURITY_AUDIT.md`)
  - `lib/observability/logger.test.ts` (secret/private-data
    scrubbing, mirrored in `TEST_EVIDENCE.md`)
- 30-step UAT that touches the same services end-to-end:
  [`UAT_RELEASE.md`](./UAT_RELEASE.md) § "2026-08-21 — 30-step
  separated-account UAT" (invitation delivery, magic-link,
  password-reset, AI toggle).
- Production deploy / migration / backup chain:
  [`MIGRATION_DEPLOYMENT.md`](./MIGRATION_DEPLOYMENT.md).
