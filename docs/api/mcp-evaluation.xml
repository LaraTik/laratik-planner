<evaluation>
  <server>laratik-planner</server>
  <endpoint>https://planner.laratik.com/api/mcp</endpoint>
  <case id="01">Without Authorization, POST initialize returns HTTP 401 and does not disclose workspace data.</case>
  <case id="02">A revoked token returns HTTP 401 on the next request.</case>
  <case id="03">A token with content:read can list workspaces and content but cannot invoke a write tool.</case>
  <case id="04">A content:write token still cannot read or mutate a workspace outside the token owner's active internal access.</case>
  <case id="05">list_content rejects invalid UUIDs, dates, statuses, formats, and limits before the domain query runs.</case>
  <case id="06">reschedule_content changes only plannedPublishAt and preserves the workflow status and creative fields.</case>
  <case id="07">transition_content delegates invalid workflow actions to the existing state machine and returns an actionable error.</case>
  <case id="08">archive_content requires confirm=true and soft-archives only the selected item.</case>
  <case id="09">Repeated identical reschedule and owner-change calls are safe and do not create duplicate state changes.</case>
  <case id="10">After 120 requests in one minute for a token, the next request returns HTTP 429 with Retry-After.</case>
  <case id="11">export_brand_kit returns every brand-kit kind for an accessible workspace; logo binaries are referenced via short-lived signed download URLs (storage_path + token + expiresAt), not raw bytes.</case>
  <case id="12">import_brand_kit with conflict_strategy='merge' skips existing logo/color/font/voice-rule/publishing-rule/linked-resource names and reports the skipped list; new rows are still created.</case>
  <case id="13">import_brand_kit with conflict_strategy='fail' rejects on the first duplicate name and does not partially apply.</case>
  <case id="14">import_brand_kit with conflict_strategy='overwrite' requires confirm=true; without confirm the request is rejected by input validation. With confirm=true, the existing row is soft-archived and the new row is created.</case>
  <case id="15">import_brand_kit logo entries accept exactly one of external_url / source_url / base64. Submitting none, or more than one, fails input validation with a clear message.</case>
  <case id="16">import_brand_kit rejects logo source_url fetches that exceed the 10 MB cap or return non-2xx; the failure is reported per-row in `failed` without aborting the rest of the import.</case>
  <case id="17">export_brand_kit and import_brand_kit both reject requests against a workspace the token owner cannot access (canAccessInternalWorkspace / workspace_manager role), returning the existing not-found / permission-denied error shape.</case>
  <case id="18">list_workspaces without `name_query` returns every accessible workspace (cap 200); with `name_query` (case-insensitive substring, 1–120 chars after trim) returns only the matching subset and still respects canAccessInternalWorkspace.</case>
  <case id="19">list_workspaces with `name_query` containing SQL LIKE wildcards (`%` / `_`) treats them as literals (ESCAPE '\\') — the result set is not widened beyond the literal substring.</case>
</evaluation>
