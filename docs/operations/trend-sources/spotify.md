# Spotify — coming soon

> **Status**: stub. This source is in the catalog but the v1 extractor is not yet graduated from `experimental` to `paid` or `free`. See the [Trend Radar source catalog](../TREND_RADAR.md#per-source-setup) for the full list of available sources.

Spotify trend discovery will use the official Spotify Web API (currently free for read-only endpoints). The sidecar already has placeholder env vars (`SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET`) in `app/config.py`. Once the team finishes the extractor, this page will document the key setup, common errors, and runbook.

If you need Spotify trends today, use one of the [1.0 sources](../TREND_RADAR.md#per-source-setup) and re-evaluate in 6 weeks.
