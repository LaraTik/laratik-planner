/**
 * Drizzle schema barrel — re-exports the full per-domain schema split.
 * This file used to be empty (Goal 0); Goal 1 fills it with the full
 * STUDIOFLOW_MASTER_PROMPT.md §8 port (15 enums, 30+ tables).
 */
export * from "./schema/index";
